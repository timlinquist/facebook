%h2 Please select one of your friends to score their commenters:

= form_for :friend, :url => friend_path, :method => :post do |form|
  = form.select :id, @friends.collect{|f| [f['name'], f['id']] }

  %br
  %br

  = form.submit 'Next'
